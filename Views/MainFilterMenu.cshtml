<div class="list-group" id="list-tab" role="tablist">
    <div class="align-self-center">
        <h4 class="text-center mb-3">Filters</h4>
        @if (ViewBag.FiltersBy == nameof(Order))
        {
            @await Html.PartialAsync("~/Views/Order/FilterMenu.cshtml")
        }
        @if (ViewBag.FiltersBy == nameof(OrderItem))
        {
            @await Html.PartialAsync("~/Views/OrderItem/FilterMenu.cshtml")
        }
        @if (ViewBag.FiltersBy == nameof(Provider))
        {
            @await Html.PartialAsync("~/Views/Provider/FilterMenu.cshtml")
        }       
    </div>
</div>
